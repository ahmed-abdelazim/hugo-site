stages:
  - build
#  - deploy
#  - test
#  - move
#  - clean

job 1:
  stage: build
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - public/
  image:
    name: klakegg/hugo
    entrypoint: ["/bin/sh", "-c"]
  script: hugo
  
job 2:
  stage: build
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - public/
  image:
    name: klakegg/hugo
    entrypoint: ["/bin/sh", "-c"]
  script: ls /builds/ahmed-abdelazim/hugo-site/public
